require 'spec_helper'

describe Instagramrb do
  it 'has a version number' do
    expect(Instagramrb::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
